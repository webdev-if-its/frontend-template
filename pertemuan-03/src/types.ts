// Tipe data bersama, dipakai di beberapa komponen pertemuan ini. Sudah
// lengkap (bukan TODO) — fokus pertemuan ini bukan union type (itu materi
// pertemuan 2), tapi list rendering, conditional rendering, Tailwind, dan
// komponen reusable.
export type Tugas = {
  id: string
  teks: string
  selesai: boolean
}
